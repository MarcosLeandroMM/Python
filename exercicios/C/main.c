#include <stdio.h> // arquivo pré-processador que roda antes do compilador, para incluir a biblioeteca de I/O. No caso a funcção printf pertece a esta biblioteca

// A função é main é primeira função a ser executada em C. Todo programa em C deve ter uma função main
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!");
   return 0; // determina o fim do programa
}

