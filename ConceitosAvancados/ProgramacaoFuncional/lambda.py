"""Expressões Lambda:

As expressões lambda permitem criar funções anônimas (sem nome) em Python. Elas são úteis quando você precisa de uma função simples para uma única tarefa.

Exemplo: 
"""

dobrar = lambda x: x * 2
resultado = dobrar(5)
print(resultado)  # Saída: 10
